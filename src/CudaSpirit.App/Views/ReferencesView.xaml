<UserControl x:Class="CudaSpirit.App.Views.ReferencesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf">
    <UserControl.Resources>
        <!-- Small site-shortcut pill -->
        <Style x:Key="SitePill" TargetType="Button">
            <Setter Property="Foreground" Value="{DynamicResource InkBrush}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,0,6,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b" CornerRadius="4" Padding="11,6" BorderThickness="1" BorderBrush="#33FFFFFF"
                                Background="{DynamicResource BgPanelHiBrush}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="BorderBrush" Value="{DynamicResource EmberBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="NavIcon" TargetType="Button" BasedOn="{StaticResource SitePill}">
            <Setter Property="MinWidth" Value="34"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Site shortcuts -->
        <WrapPanel Grid.Row="0" Margin="0,0,0,8">
            <TextBlock Text="🌐" FontSize="16" VerticalAlignment="Center" Margin="0,0,10,6"/>
            <Button Style="{StaticResource SitePill}" Content="Official Updates" Tag="https://www.naeu.playblackdesert.com/en-US/News/Notice?boardType=2" Click="OnGoSite" Margin="0,0,6,6"/>
            <Button Style="{StaticResource SitePill}" Content="Garmoth" Tag="https://garmoth.com" Click="OnGoSite" Margin="0,0,6,6"/>
            <Button Style="{StaticResource SitePill}" Content="QuestLog" Tag="https://questlog.gg" Click="OnGoSite" Margin="0,0,6,6"/>
            <Button Style="{StaticResource SitePill}" Content="BDO Codex" Tag="https://bdocodex.com/us/" Click="OnGoSite" Margin="0,0,6,6"/>
            <Button Style="{StaticResource SitePill}" Content="Bdolytics" Tag="https://bdolytics.com/en/NA" Click="OnGoSite" Margin="0,0,6,6"/>
            <Button Style="{StaticResource SitePill}" Content="World Map" Tag="https://bdocodex.com/us/map/" Click="OnGoSite" Margin="0,0,6,6"/>
            <Button Style="{StaticResource SitePill}" Content="Arsha API" Tag="https://documenter.getpostman.com/view/4028519/TzK2bEVg" Click="OnGoSite" Margin="0,0,6,6"/>
            <Button Style="{StaticResource SitePill}" Content="BDO Alerts API" Tag="https://api.bdoalerts.net/docs" Click="OnGoSite" Margin="0,0,6,6"/>
            <Button Style="{StaticResource SitePill}" Content="Local Data Extractor" Tag="https://github.com/lukeswitz/Black-Desert-Online-Data-Extractor" Click="OnGoSite" Margin="0,0,6,6"/>
        </WrapPanel>

        <!-- Address bar + nav -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <Button x:Name="BackBtn"   Style="{StaticResource NavIcon}" Content="◀" Click="OnBack"   ToolTip="Back"/>
                <Button x:Name="FwdBtn"    Style="{StaticResource NavIcon}" Content="▶" Click="OnForward" ToolTip="Forward"/>
                <Button x:Name="ReloadBtn" Style="{StaticResource NavIcon}" Content="⟳" Click="OnReload"  ToolTip="Reload"/>
            </StackPanel>
            <TextBox x:Name="UrlBox" Grid.Column="1" Margin="6,0" VerticalContentAlignment="Center"
                     MinHeight="34" KeyDown="OnUrlKey" ToolTip="Type a URL and press Enter"/>
            <Button Grid.Column="2" Style="{StaticResource GlowButton}" Content="Go" Click="OnGo"/>
        </Grid>

        <!-- Browser -->
        <Border Grid.Row="2" Style="{StaticResource Card}" Padding="2">
            <wv2:WebView2 x:Name="Web"/>
        </Border>
    </Grid>
</UserControl>
